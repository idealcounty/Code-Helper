# Code Helper 后续优化路线图

## 1. 文档目的

本文基于仓库根目录外的《Coding Agent 开发流程与项目方案》、当前 `Code-Helper` 实现、提交历史和自动化测试，回答两个问题：

1. 已经完成的能力中，哪些仍需要从“基础可用”加固到“可信可用”。
2. 在不偏离原开发流程的前提下，下一阶段最值得实现什么。

本文不是重新设计项目，也不把业界产品功能清单直接搬进来。所有建议都服从原开发流程的核心判断：

> 一个功能只有在它能提高真实编程任务完成能力，或提高自主操作可信度时，才值得占用开发时间。

## 2. 必须保持的边界

后续优化继续遵守以下约束：

- Agent Loop、状态管理、上下文构造、Tool Calling 解析、工具执行、权限、恢复和终止逻辑继续自行实现。
- 不封装 Codex、Claude Code、Cline、Aider、DeepSeek Harness 等现有 Agent 产品。
- 不引入 LangChain、LangGraph、OpenAI Agents SDK、AutoGen、CrewAI 等 Agent 框架或 SDK。
- 不用云端 Code Interpreter、Files API 或远程执行服务替代本地工具。
- 不把多 Agent、完整容器沙箱、插件市场、自动提交/推送/PR、多模型路由作为近期目标。
- 保持“只追加事件日志”“读取后修改”“统一权限管线”“修改后验证”“轻量检查点”这些已有设计，不用新功能绕过它们。
- 继续优先保证 CLI 和本地 Web 使用同一个 Agent Core。

## 3. 当前完成基线

截至 2026-08-29，仓库全量测试为 `129 passed`。以下主闭环已完成，不建议重复建设：

| 能力 | 当前状态 | 主要实现 |
| --- | --- | --- |
| 自研 Agent Loop | 已完成 | `agent_loop.py`、`session.py` |
| 原生 Tool Calling 与流式响应 | 已完成 | `model.py` |
| 工作区文件、搜索、Patch、命令和 Git Diff | 已完成 | `tools/` |
| Ask / Plan / Act 与统一审批 | 已完成基础版 | `permissions.py`、`agent_loop.py` |
| 读取哈希和外部修改保护 | 已完成 | `tools/workspace.py`、`tools/filesystem.py` |
| JSONL 事件和 Session 恢复 | 已完成基础版 | `events.py`、`AgentState.restore_from_events` |
| 验证新鲜度和有限修复 | 已完成基础版 | `verifier.py`、`agent_loop.py` |
| 检查点、Diff 和回滚 | 已完成基础版 | `checkpoints.py`、Web API |
| Repo Map Lite、Skills、动态计划 | 已完成基础版 | `repo_map.py`、`skills.py`、`tools/plan.py` |
| 上下文裁剪、摘要和完整工具输出引用 | 已完成基础版 | `context.py`、`tool_executor.py` |
| Web UI、WebSocket、可选桌面入口 | 已完成 | `web/`、`desktop.py` |
| 项目级和用户级长期记忆 | 已完成并持续加固 | `memory.py`、`memory_summary.py`、`user_memory.py` |

目前最值得投入的方向不是再增加一个大模块，而是修复“文档承诺强于实现保证”的部分。

## 4. 优先级总览

| 编号 | 优先级 | 优化项 | 主要收益 | 建议工作量 |
| --- | --- | --- | --- | --- |
| R1 | P0 | 真正可中止的运行预算（已完成 V1） | 停止后不再继续模型或子进程，避免失控 | 中 |
| R2 | P0 | 事件归约器与可恢复审批 | 重启后不重复副作用，恢复状态可信 | 中高 |
| R3 | P0 | 验证证据加固（已完成 V1） | 防止任意成功命令伪装成测试通过 | 中 |
| R4 | P0 | 权限能力与审批策略分离 | 降低黑名单绕过和过度询问 | 中高 |
| R5 | P0 | 冲突安全的检查点恢复（已完成 V1） | 防止回滚覆盖用户后续修改 | 中 |
| R6 | P0 | 可重复 Eval 基准与质量门禁（已完成 V1） | 用真实指标判断 Agent 是否进步 | 中 |
| R7 | P0 | 日志完整性与敏感信息脱敏（已完成 V1） | 审计可用且不泄露凭据 | 中 |
| R8 | P1 | 路径相关的 `AGENTS.md` 规则链（V1 已完成） | 避免无关目录规则污染任务 | 中 |
| R9 | P1 | Repo Map 依赖图与预算化注入（V1 已完成） | 减少盲目搜索，提高跨文件定位率 | 中高 |
| R10 | P1 | 高保真上下文压缩 | 长任务不丢目标、失败和验证证据 | 中高 |
| R11 | P1 | 可配置但受控的生命周期 Hooks（V1 已完成） | 将已有空管线变成真实工作流能力 | 中 |
| R12 | P1 | 命令增量输出与统一运行可观测性（V1 已完成） | 长命令更易停止、诊断和展示 | 中 |
| R13 | P2 | 安全只读工具并行 | 降低仓库探索延迟 | 中 |
| R14 | P2 | 多语言符号提取与增量 Repo Map 缓存 | 改善非 Python 仓库理解 | 中高 |

## 5. P0：先补齐可信执行

### R1. 真正可中止的运行预算

**实施状态：V1 已完成（2026-08-28）**

- 共享 `CancellationToken` 可中断模型请求、审批等待和 Tool Task；Web 停止接口发布请求与完成事件。
- `RunBudget` 统一执行 wall-time、Step 和供应商回报 Token 预算，耗尽后以带原因的 `PARTIAL` 结束。
- shell 在 Windows 使用 `taskkill /T /F`、在 POSIX 使用独立进程组终止子进程树；超时和取消返回不同结构化代码。
- Web“智能”面板展示 Step、时间和 Token 遥测，“轨迹”面板展示停止与预算耗尽原因。
- 已有模型请求中断、Web 端到端停止、时间/Token 门禁和 Windows 子进程树清理测试。

已知边界：供应商仅在响应结束后返回 usage 时，Token 是请求间硬门禁，不能在单次生成的任意 Token 点精确中断。Windows 本地与 Ubuntu CI 已通过同一子进程树测试。

**当前差距**

- 当前预算是单 Turn 范围，不包含跨恢复 Session 的累计费用上限。
- 模型流的 Token 硬上限仍受供应商协议和 `max_tokens` 能力约束。
- 尚未记录停止请求到模型或进程实际退出的延迟分布。

**后续加固**

- 对支持单次输出上限的供应商下传剩余生成预算。
- 记录停止请求到模型/进程真正退出的延迟指标。

**验收标准**

- 点击停止后，模型流和测试进程在可量化时间内结束，状态稳定为 `CANCELLED`。
- 达到时间或 Token 预算时进入 `PARTIAL`，事件中明确是 `TIME_BUDGET_EXHAUSTED` 或 `TOKEN_BUDGET_EXHAUSTED`。
- Windows 和 Linux 均有子进程树取消测试，不遗留后台进程。

### R2. 事件归约器与可恢复审批

**实施状态：V1 已完成（2026-08-29）**

- 新增 `SessionReducer.apply(event)`，在线 AgentLoop 与离线 `restore_from_events` 使用同一归约逻辑；parity 测试覆盖消息、状态、统计、预算稳定字段和验证事实。
- 事件自动携带 `schema_version`、稳定 `event_id` 和可选 `causation_id`；同一事件 ID 与同一 Tool Call ID 的重复结果不会重复计入状态或副作用事实。
- `approval_requested` 会持久化待审批 Tool Call 和剩余调用，重启后可继续调用 `resume_approval`；只有存在 `tool_result` 才会视为工具已完成。
- 恢复时发现 `tool_started` 没有对应结果，会标记 `INTERRUPTED_UNKNOWN` 并生成恢复警告，绝不自动重放工具。
- EventStore 只容忍尾部半写/损坏行并保留诊断；中间损坏仍拒绝静默恢复。Web Session/Report 会暴露待审批、未知中断和恢复警告。

**剩余差距**

- 事件尚未加入内容校验和、`causation_id` 的生产级链路关联，以及可视化的恢复诊断时间线。
- 仍需补充四个关键崩溃点的端到端重启测试，并验证跨进程/跨主机恢复时的版本兼容策略。
- `INTERRUPTED_UNKNOWN` 目前只进入恢复状态和报告，尚未提供逐个调用的人工重试/放弃 UI。

**建议实现**

- 把恢复逻辑抽成唯一的 `SessionReducer.apply(event)`，在线状态推进和离线恢复共享同一套归约规则。
- 为事件增加 `schema_version`、`event_id`、`causation_id`，Tool Call 使用稳定 id 做幂等判断。
- 从 `approval_requested` 与 `approval_result` 重建待审批状态；发现 `tool_started` 但没有 `tool_result` 时标记为 `INTERRUPTED_UNKNOWN`，绝不自动重放写操作。
- 恢复修改和验证序列，保证重启后 `verification_is_fresh` 不会被错误重置。
- 对尾部半写事件做容错：保留此前有效事件，隔离损坏行并发布恢复诊断；中间损坏则拒绝静默恢复。

**验收标准**

- 在“等待审批、文件刚写完、命令运行中、验证刚完成”四个位置模拟崩溃，恢复后都不会重复执行副作用。
- 同一 Tool Call id 最多产生一个成功副作用结果。
- 恢复后的最终报告与崩溃前的事件事实一致。

### R3. 验证证据加固

**实施状态：V1 已完成（2026-08-29）**

- `VerificationEvidence` 记录命令、类型、来源、起止事件、退出码、关联文件、输出摘要、适用性与拒绝原因。
- 仅可识别的测试、构建、Lint、类型检查、编译命令，或用户原文明确指定的实质性自定义命令可以满足完成契约。
- `echo`、`pwd`、未知命令和可能用 `|| true` 掩盖失败的命令不会刷新验证新鲜度。
- 定向单测会保守拒绝无法证明覆盖全部修改文件的多文件任务；证据可跨 Session 事件恢复，并在 Web“智能”面板展示。

后续加固方向是利用依赖图和项目配置提高“相关测试”范围判定精度；在此之前宁可返回 `PARTIAL`，不推测无证据的覆盖关系。

**当前差距**

- 尚未利用导入图或覆盖率数据证明目标测试与修改文件的真实依赖关系。
- 用户自定义命令目前要求完整命令文本出现在本轮任务原文中，尚未提供独立的项目验证配置文件。

**建议实现**

- 引入 `VerificationEvidence`，至少记录命令、类型、来源、开始/结束事件序列、退出码、关联修改文件和输出摘要。
- 验证来源分为：用户明确指定、项目配置推断、相关测试推断、人工核验；只有前三类成功证据可以自动满足完成契约。
- 建立轻量验证命令识别器，识别 pytest、unittest、npm/pnpm test、构建、lint、typecheck 等；未知命令即使成功，也只算普通命令。
- 按修改文件关联验证范围。跨文件修改不能只凭无关的单个测试自动宣布完全完成。
- 最终报告展示“为何该证据适用”，而不只展示退出码。

**验收标准**

- `echo ok`、`pwd` 等命令不能被标记为自动化验证。
- 用户明确指定的自定义验证命令仍可被接受，并标记来源为 `user_requested`。
- 写入发生后，所有旧证据立即失效；恢复 Session 后仍保持相同结论。

### R4. 权限能力与审批策略分离

**实施状态：V1 已完成（2026-08-29）**

- `PermissionPolicy` 将工具请求归一化为 `workspace.read/write`、`process.exec`、`network.egress`、`dependency.install`、`path.outside_workspace` 和 `destructive.restore` 能力，并在 `tool_requested` 事件中展示。
- 能力边界先于审批策略判断：工作区外路径直接拒绝；Ask/Plan 继续拒绝写和命令能力；Act 模式对非破坏性写入与命令按既有策略询问。
- 增加仅存于当前 Runtime 的范围化会话授权，绑定能力、路径/命令前缀和过期时间；授权不会写入 Session 或跨新会话继承。
- 危险命令识别补充 PowerShell 删除别名、磁盘擦除等等价形式；网络访问、依赖安装和长时命令作为独立能力暴露并继续要求审批。

**剩余差距**

- 当前命令入口仍以 shell 字符串为主，尚未提供 argv 结构化执行器；复杂 shell 拼接仍只能 best-effort 分类。
- 会话授权已有策略对象和单测，但尚未接入 Web 的可视化“允许本会话同类操作”控件。
- `.git`、`.code-helper`、凭据路径和网络域名尚未拆成独立的可配置策略文件。

**业界借鉴**

OpenAI Codex 将 Sandbox Mode（技术上能做什么）和 Approval Policy（何时询问用户）分开，并默认关闭命令网络、限制工作区写入。这里应借鉴“能力边界与审批时机分离”，但不需要近期实现完整容器沙箱。

**建议实现**

- 将工具请求归一化为能力集合，例如 `workspace.read`、`workspace.write`、`process.exec`、`network.egress`、`path.outside_workspace`、`dependency.install`、`destructive.restore`。
- PermissionPolicy 根据模式、能力、命令分类和会话授权返回 `allow / ask / deny`。
- 为命令工具增加优先使用 `argv` 的结构化入口，保留 shell 字符串作为兼容路径并提高风险等级。
- 自动允许确定性的只读检查命令；修改、网络、安装、长时间和破坏性操作继续询问。
- 支持范围明确的“本会话允许同类操作”，授权必须包含工具、路径/命令前缀、过期时间，不能保存宽泛永久权限。
- 将 `.git`、`.code-helper` 内部状态、凭据路径和工作区外路径作为独立保护规则。

**验收标准**

- 等价的危险命令不能仅通过大小写、引号、PowerShell 别名或命令拼接绕过策略。
- Plan/Ask 模式在 Tool Schema 暴露和执行层都拒绝写能力。
- 会话授权只匹配被批准的精确范围，新会话不会继承。

### R5. 冲突安全的检查点恢复

**实施状态：V1 已完成（2026-08-29）**

- Manifest 同时保存修改前基线和每次 Agent 修改后的最新副本、哈希、工具及事件序列。
- 恢复前对全部选中文件执行原子预检；任一文件与 Agent 最后产物不一致时，普通恢复零写入并返回 `RESTORE_CONFLICT`。
- 冲突响应包含外部修改 Diff 与预计恢复 Diff；Web 要求第二次明确确认后才允许强制覆盖，确认绑定当前文件哈希，内容再次变化后旧确认自动失效。
- 支持按文件选择恢复；恢复事件记录文件、预览与是否强制，并使旧验证失效。

后续可把文件选择和 Diff 预览做成专用恢复对话框；V1 已满足“不静默覆盖”的安全不变量。

**当前差距**

- 当前 Web 使用浏览器确认框完成二次确认，尚未提供逐文件勾选的可视化恢复对话框。
- 文本 Diff 限制为 1 MB；二进制或超大文件只返回哈希冲突信息。

**业界借鉴**

Cline 提供按时间点比较和恢复文件/任务的体验。本项目应只借鉴“可比较、可选择、可追踪”的交互，不照搬 shadow Git，也不改写真实 Git 历史。

**建议实现**

- Manifest 同时记录修改前哈希、Agent 修改后哈希、事件序列、触发工具和计划快照。
- 恢复前比较当前哈希与最近 Agent 产物哈希；若不一致，返回 `RESTORE_CONFLICT` 并展示 Diff，要求用户再次明确确认。
- 支持预览恢复内容和按文件选择恢复；首版仍保留每 Turn 一份轻量基线。
- 恢复本身作为一次 Mutation，旧验证立即失效，并发布完整恢复事件。

**验收标准**

- Agent 修改后用户再编辑同一文件时，普通恢复不会静默覆盖用户内容。
- 恢复新建文件、删除文件和多文件修改均可重复测试。
- 恢复后的 Diff、验证状态和事件日志一致。

### R6. 可重复 Eval 基准与质量门禁

**实施状态：V1 已完成（2026-08-29）**

- `evals/tasks/` 提供 10 个版本化 JSON 契约，覆盖项目问答、单文件缺陷、跨文件功能、并发修改、拒绝审批、检查点恢复、卡死终止、长输出取消、Session 中断和敏感环境。
- 确定性 `ScriptedModel` 复用真实 Runtime、AgentLoop、权限、工具和事件管线，一条命令生成 JSON/Markdown 报告。
- 首份基线为：契约通过率、可完成任务完成率、安全拦截率、修改后新鲜验证率、Recall@5 与首次相关文件命中率均为 100%；平均 3.4 Steps、2.6 次工具调用，脚本化 Token 共 952。
- CI 在 Python 3.11 上比较 `evals/reports/baseline.json`，任一核心比率回退、基线任务缺失或质量门禁失败都会阻断，并上传当次报告。
- 真实模型模式必须显式传入 `--allow-paid`，记录供应商、模型、推理档位、Prompt 哈希、代码提交和可选费用估算，但不作为唯一 CI 门禁。

**当前边界**

- 确定性结果验证的是 Agent 契约和执行管线，不代表真实模型智能水平。
- 真实模型首版只启用项目问答、单文件缺陷和跨文件功能三类任务，其余安全场景仍由确定性层控制变量。
- `sensitive_environment` 当前只证明命令环境不会继承 API Key；运行时统一脱敏已在 R7 V1 落地，完整格式覆盖和配额仍待增强。
- Session 中断任务证明副作用不会被自动重放；等待同一审批的完整恢复仍属于 R2。

**建议实现**

- 新建 `evals/`，每个任务包含初始仓库 fixture、用户任务、允许能力、期望文件状态、验证命令和安全断言。
- 至少覆盖项目问答、单文件缺陷、跨文件功能、并发修改、拒绝审批、回滚、卡死、长输出、Session 恢复和敏感信息十类场景。
- 提供确定性的 ScriptedModel 回归和可选真实模型评测；CI 默认只跑确定性版本，真实模型需要显式 Key 和预算。
- 生成 JSON 与 Markdown 报告，记录完成率、安全拦截率、修改后验证率、平均 Step、Token、耗时和失败分类。
- 每个优化 PR 必须对比基线，不能只报告“测试通过”。

**验收标准**

- 基准任务可一条命令重复运行，失败能定位到具体任务和契约。
- 安全用例拦截率为 100%，有验证条件的修改任务验证率为 100%。
- 真实模型结果保存模型名、配置和随机性条件，避免不可比较数据。

### R7. 日志完整性与敏感信息脱敏

**实施状态：V1 已完成（2026-08-29）**

- `Redactor` 统一处理事件持久化、WebSocket 事件和完整工具输出引用，支持 API Key、Bearer、常见供应商 Key、URL 凭据、敏感字段和值注入脱敏。
- 新事件写入 `integrity_sha256`；加载时校验完整性，尾部半写/篡改隔离并产生诊断，中间损坏拒绝静默恢复。
- 运行时保留原始值用于必要的 Agent 协议和状态推进，但不会把原始事件或完整命令输出发给 UI 或落盘。
- `sensitive_environment` Eval 与脱敏专项测试覆盖事件监听、事件文件和完整工具输出引用。

**剩余差距**

- 当前是 best-effort 模式，尚未覆盖所有第三方凭据格式、二进制输出和跨进程密钥注入；用户仍不应把凭据放进提示词或命令行。
- 事件内容校验目前只保护新写入事件，旧无签名事件会兼容加载并标记 `LEGACY_EVENT_UNSIGNED`，不会自动改写历史文件。
- 尚未设置单 Session/目录磁盘配额和自动清理策略。

**建议实现**

- 在 EventStore 写入前增加统一 `Redactor`，处理常见 API Key、Authorization header、URL 凭据和配置字段；保留原始数据的功能必须显式关闭且默认不可用。
- 为每行事件增加内容校验和；Session 报告显示事件缺口、损坏和恢复警告。
- 工具完整输出引用同样经过脱敏，并设置单 Session/目录磁盘配额和清理策略。
- 增加仓库内凭据扫描测试和运行时日志脱敏测试。

**验收标准**

- 将模拟 Key 放入用户消息、命令参数、stderr 和模型错误，所有持久化文件中都只能看到掩码。
- 日志篡改或中间缺行可被检测，不能以“完整轨迹”展示。

## 6. P1：提高仓库理解和工作流质量

### R8. 路径相关的 `AGENTS.md` 规则链

**实施状态：V1 已完成（2026-08-29）**

`ContextManager` 已改为依据当前变更文件和最近工具调用中的路径推断目标目录，只加载根目录到目标目录的规则链；无明确目标时退化为仅加载工作区根规则。无关子目录规则不再进入上下文。

**业界借鉴**

OpenAI Codex 的公开规则是从项目根目录沿当前工作目录向下构造指令链，每层只选一个规则文件，越靠近目标目录优先级越高，并设置总字节上限。

**已实现**

- 根据 `changed_files` 和最近 `read_file`/`write_file`/`apply_patch` 等动作参数构造 `root -> target directory` 规则链。
- 每层优先 `AGENTS.override.md`，否则读取 `AGENTS.md`；同层不同时加载两份。
- 增加独立规则字符预算（默认 20,000）、单文件上限和来源数量上限，记录来源、作用域、类型和截断状态。
- `context_built` 事件记录规则候选数、实际字符数、是否截断及生效来源，供轨迹和后续 Web 展示使用。

**已知边界与后续加固**

- 当前路径推断主要依赖结构化工具参数和变更文件，尚未解析用户自然语言中的多个目标路径。
- 多目标目录的规则冲突目前通过根到深层的确定顺序处理，尚未提供专门的冲突标注 UI。

### R9. Repo Map 依赖图与预算化注入

**实施状态：V1 已完成（2026-08-29）**

Repo Map 现在会在原有文件名、类型和 Python 符号评分上叠加轻量导入图中心性：被多个 Python 文件导入的模块获得额外权重，并在结果中提供 `dependencies`、`dependents` 和 `centrality`。ContextManager 会把排序后的摘要按独立字符预算注入每次模型上下文。

**业界借鉴**

Aider 的 Repo Map 使用文件依赖图、关键符号和动态 Token 预算选择最相关部分。本项目只需实现轻量版本，不需要复制完整算法。

**已实现**

- 解析 Python `import`/`from` 边，使用引用次数作为可解释的中心性近似，并兼容测试文件的依赖边。
- 综合任务关键词、导入中心性和最近变更文件评分；结果保留每个文件的原因列表。
- Repo Map 使用独立字符预算，超出预算时停止追加并标记 `truncated`。
- `context_built` 事件记录查询、候选数、选中文件、分数/原因、预算和截断状态。

**已知边界与后续加固**

- 当前依赖解析是 Python 轻量启发式，尚未覆盖动态导入、别名重导出和 Java/TypeScript 等语言。
- 每次构造上下文会重新扫描文件，尚未以文件哈希做增量缓存；只有在真实仓库探索成为瓶颈时再实现缓存。

### R10. 高保真上下文压缩

**实施状态：V1 已完成（2026-08-29）**

Step 级历史裁剪现在生成确定性的结构化摘要，并将摘要元数据写入 `AgentState` 与 `context_compacted` 事件；Turn Summary 仍作为结束后的长期记忆候选来源，两者职责保持分离。

**已实现**

- 压缩输入来自当前 reducer 投影出的事件事实，而不是模型自述；摘要包含目标、模式约束、已改文件、工具证据、计划、验证、审批/中断和下一步。
- 摘要带 `version`、覆盖消息范围、覆盖事件序列和字符数，并随 `context_compacted` 事件持久化，重启后可恢复。
- 摘要有固定 6,000 字符上限；原始事件和最近消息仍保留，压缩只影响模型请求上下文。
- Web 智能面板展示本 Step 上下文来源和摘要覆盖范围，轨迹展示上下文构建与压缩事件。

**已知边界与后续加固**

- 当前使用字符预算作为稳定 Token 代理，尚未接入供应商 tokenizer；供应商 usage 仍仅用于运行预算统计。
- 摘要为确定性模板，不调用额外摘要模型，因此表达紧凑性优先于自然语言概括；后续可在 Eval 证明收益后增加可选模型摘要。

### R11. 可配置但受控的生命周期 Hooks

**实施状态：V1 已完成（2026-08-29）**

`HookManager` 现在支持 Pre/Post Tool Hook 以及 `OnVerification`、`OnTaskEnd` 两类生命周期 Hook。Runtime 默认注册的内部 Hook 只提供验证失败和任务结束的附加上下文，不改变 Agent 状态或权限决定。

**业界借鉴**

Claude Code Hooks 使用明确事件、matcher、结构化 JSON 输入输出、超时和决策结果。可借鉴协议清晰度，但本项目首版只支持少量本地 Hook，不开放任意插件系统。

**已实现**

- 支持 `PreToolUse`、`PostToolUse`、`OnVerification`、`OnTaskEnd` 四种事件；Pre Hook 可结构化拒绝，拒绝仍经 `ToolExecutor` 归一化为工具结果。
- Hook 返回结构化 `allow / reason / code / additional_context`；生命周期决策写入 `hook_executed`，附加上下文写入 `hook_context` 并可恢复。
- Runtime 默认启用安全的验证失败提示和任务结束证据提示；Hook 没有 PermissionPolicy、工具执行或审批权限。
- Web 智能面板显示四类 Hook 的注册数量和管线状态。

**已知边界与后续加固**

- V1 只提供 Python 内部回调配置，尚未开放外部命令型 Hook 的 matcher、argv、超时和独立工作目录。
- Hook 失败会被记录并保持主流程可继续；是否因 Hook 失败中止任务仍需结合真实工作流 Eval 决定。

### R12. 命令增量输出与统一运行可观测性

**实施状态：V1 已完成（2026-08-29）**

shell 现在并发读取 stdout/stderr，并在进程运行期间通过 Agent EventBus 发布增量事件；最终结果仍保留统一摘要和完整输出引用。

**已实现**

- 并发读取 stdout/stderr，发布带 Tool Call ID、流类型和顺序号的 `tool_output_delta`；最终 `tool_result` 标记 `output_streamed`，前端避免重复渲染。
- 每次读取最多 4 KiB，完整内容仍由原有工具结果存储和脱敏引用机制处理；观察器异常不会改变命令结果。
- Web 终端实时显示增量，智能接口统计增量事件数量，WebSocket 继续使用 sequence 去重和补发。

**已知边界与后续加固**

- V1 仍按读取块推送，尚未实现按时间窗口的 UI 速率限制；极高频输出时可在 EventBus 层增加批量合并。
- 模型、审批、Hook 和上下文构造的细粒度耗时尚未统一成单独 Span，当前仍由已有事件和工具统计提供可观测性。

## 7. P2：收益明确后再做

### R13. 安全只读工具并行

只并行满足以下条件的调用：ToolRisk 为 READ、参数路径互不依赖、没有 Hook 声明串行要求、结果顺序可按原 Tool Call index 重排。写操作、命令、记忆写入和审批操作继续严格顺序执行。先用 Eval 证明仓库探索延迟显著下降，再默认启用。

### R14. 多语言符号提取与增量缓存

在 Python AST 稳定后，可按真实演示仓库需要增加 JavaScript/TypeScript、Java 或 Go 的轻量符号提取。优先使用语言自带解析器或小型语法库，不为“支持语言数量”引入大型 IDE 索引系统。每增加一种语言，都要提供符号准确率和上下文预算测试。

## 8. 明确不建议近期实施

- 多 Agent 分工、Agent Team 或自动委派。
- 完整 Docker/远程沙箱平台；近期只加固本地权限和进程边界。
- Cline 式 shadow Git；继续使用轻量文件检查点。
- 任意第三方 Hook/插件下载和插件市场。
- IDE 级语言服务器、复杂多窗口编辑器和账号系统。
- 自动提交、推送、创建 PR 或修改远端仓库。
- 复杂多模型路由、自动切换供应商和为模型数量而增加配置。
- 在没有 Eval 数据前重写 Agent Loop 或更换前端技术栈。

这些内容要么被原开发流程明确排除，要么会显著扩大攻击面和工程范围，却不能直接提高当前演示闭环的可信度。

## 9. 推荐实施顺序

### 迭代 1：停止与恢复必须可信

完成 R1、R2、R5。演示“运行中取消”“等待审批后重启”“外部修改后拒绝覆盖式回滚”。这是当前最明显的可靠性提升。

### 迭代 2：完成结论必须可信

完成 R3、R4、R7。演示伪验证被拒绝、范围化会话授权、敏感信息不会写入事件日志。

### 迭代 3：用数据驱动继续开发

完成 R6，建立 8～10 个任务基线。之后任何 Repo Map、上下文、Prompt、记忆或模型配置改动都必须提供 Eval 对比。

### 迭代 4：提升任务成功率和体验

按 Eval 结果选择 R8～R12，不要求一次全部完成。只有当探索耗时成为明确瓶颈时，再实现 R13；只有真实目标仓库需要时，再实现 R14。

## 10. 下一阶段完成定义

下一阶段不是以“又增加多少功能”验收，而以以下事实验收：

- [ ] 用户停止后没有模型流、Hook 或子进程继续运行。
- [ ] 任意关键点重启不会重复写文件或执行命令。
- [x] 未知成功命令不能冒充测试、构建或静态检查证据。
- [x] 权限决策同时考虑能力边界和审批策略，核心安全用例拦截率为 100%（argv 入口和策略文件仍待增强）。
- [ ] 检查点恢复不会静默覆盖 Agent 之后的外部修改。
- [x] 新事件与完整工具输出中的模拟凭据脱敏，尾部事件损坏可诊断（旧无签名事件和磁盘配额仍有边界）。
- [x] 8～10 个 Eval 任务可重复执行，完成率和失败原因可对比。
- [x] 有自动验证条件的修改任务，修改后验证率为 100%。
- [ ] CLI、Web 和可选桌面入口仍使用同一 Agent Core 和事件事实源。
- [ ] 全量单元、集成、安全测试通过，演示任务在提交环境重新运行并留存证据。

## 11. 参考与借鉴边界

以下资料只用于借鉴公开机制，不作为依赖或可执行指令：

- [原开发流程列出的 DeepSeek Harness、Codex、Claude Code、Aider、Cline、Gemini CLI、OpenHands、OpenCode 资料](../../Coding%20Agent开发流程.md)
- [OpenAI Docs：Agent approvals & security](https://developers.openai.com/codex/agent-approvals-security)，借鉴 Sandbox 与 Approval 分层、默认工作区和网络边界。
- [OpenAI Docs：Custom instructions with AGENTS.md](https://developers.openai.com/codex/agent-configuration/agents-md)，借鉴根目录到目标目录的规则链、override 和大小上限。
- [Aider Repository Map](https://aider.chat/docs/repomap.html)，借鉴依赖图排名、关键符号和动态 Token 预算。
- [Claude Code Hooks Reference](https://code.claude.com/docs/en/hooks)，借鉴明确事件、matcher、结构化输入输出和超时。
- [Cline Checkpoints](https://docs.cline.bot/core-workflows/checkpoints)，借鉴按时间点比较和恢复的交互；不采用 shadow Git 实现。

所有实现仍需满足本文第 2 节的自研和范围边界。
