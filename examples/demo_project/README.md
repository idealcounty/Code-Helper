# Code Helper 演示工程

这是一个最小的真实编程任务：`greeting.py` 当前返回旧格式，`test_greeting.py` 明确要求产品格式。

建议演示提示词：

> 阅读项目并修复 greet 的返回格式，使测试通过。先说明计划，修改后运行 pytest，并展示最终 Diff 和验证结果。

在项目根目录执行：

```powershell
python -m pytest examples/demo_project -q
```

演示重点：Repo Map/搜索 → 读取文件 → 动态计划 → 用户批准修改 → 测试验证 → Diff 与证据报告。
